spring:
  application:
    name: SiegeStats

  datasource:
    url: jdbc:postgresql://postgres:5432/SiegeStats
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false